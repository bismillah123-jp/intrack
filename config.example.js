// Copy this file to config.js and replace both values with your Supabase project credentials.
// The app imports this file as an ES module when it is served over http(s).

export const SUPABASE_URL = "https://your-project.supabase.co";
export const SUPABASE_ANON_KEY = "your-anon-key";
export const AI_EXECUTE_URL = "/api/v1/ai-execute";

window.DOMPETRAPI_CONFIG = {
  SUPABASE_URL,
  SUPABASE_ANON_KEY,
  AI_EXECUTE_URL
};
